---
title: Workflow Studio
description: Build multi-step content pipelines — research, write, image, publish — as visual workflows you can save and re-run.
---

# Workflow Studio <span className="badge badge--pro">PRO</span>

Workflow Studio chains Auto RepAI's capabilities into **repeatable pipelines**.

## How it works

1. Go to **Auto RepAI → Workflow Studio**.
2. Compose steps — e.g. *research a topic → draft the article → generate a featured image → create the post*.
3. **Save** the workflow with a name; your saved workflows are listed for one-click re-runs.
4. **Execute** — steps run in order, passing their output forward; the result can be published or saved as a draft for review.

Workflows are yours: each user's saved workflows are scoped to their account.

## When to use it vs Automations

- **Workflow Studio** = *on-demand, multi-step*: "run my launch-article pipeline for this topic, now."
- **[Automations](/pro/automations)** = *scheduled, single-purpose*: "two posts a week from this topic list, forever."

They combine well: design the perfect sequence in Workflow Studio, then let Automations handle the recurring simple cases.
